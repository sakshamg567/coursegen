import * as React from "https://esm.sh/react@18";
export default React;

// Re-export hooks
export const {
  useState,
  useEffect,
  useRef,
  useMemo,
  useCallback,
  createContext,
  useContext,
} = React;
